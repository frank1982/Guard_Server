<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.westwind.guard.dao.AdminDao">
<select id="getAdmin" parameterType="org.westwind.guard.model.Admin" resultType="org.westwind.guard.model.Admin">
    SELECT * FROM admin_tb WHERE name=#{name} AND pwd=#{pwd}
</select>
</mapper>